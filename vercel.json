{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm db:generate && pnpm --filter @doodle-dash/web build && rm -rf .next && cp -a apps/web/.next .next && cp -f apps/web/next.config.js next.config.js",
  "git": {
    "deploymentEnabled": true
  }
}
