{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "DATABASE_URL=postgresql://preview:preview@127.0.0.1:5432/preview pnpm db:generate && pnpm --filter @doodle-dash/web build && rm -rf .next && cp -a apps/web/.next .next && cp -f apps/web/next.config.js next.config.js"
}
