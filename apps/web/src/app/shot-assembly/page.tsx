import { ShotAssemblyPlanner } from '@/components/preview/ShotAssemblyPlanner';

export const dynamic = 'force-dynamic';

export default function ShotAssemblyPage() {
  return <ShotAssemblyPlanner />;
}
