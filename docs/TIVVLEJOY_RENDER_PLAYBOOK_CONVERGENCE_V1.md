# Render playbook convergence

Zero-GPU planning → preview → visual approval → cost estimate → explicit paid
authorization → one exact render job → receipt → QC.

This marathon issues no authorization.
