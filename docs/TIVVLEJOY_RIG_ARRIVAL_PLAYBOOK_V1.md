# TIVVLEJOY_RIG_ARRIVAL_PLAYBOOK_V1

Exact future steps when Michael returns Pip and Goat. Do **not** run these against nonexistent files. Do **not** auto-replace the prior version.

1. **RECEIVE** the external production rig files outside this planner.
2. **HASH** the immutable source bytes. Identity is the hash, not the filename.
3. **REGISTER IMMUTABLE SOURCE** as a receipt (`sourceReceiptRef` + `sourceSha256`).
4. **INSPECT** in a future authorized Blender session. Record armature, bones, controls, shape keys, constraints, drivers, IK/FK, attachments, materials, meshes, weights, unsupported dependencies, duplicates, unbound meshes, and scale concerns.
5. **CAPABILITY REPORT** against the Pip or Goat semantic profile. Missing required families stay `RIG_CAPABILITY_INCOMPLETE`.
6. **TEST POSE REPORT** using the deterministic matrix (neutral, expressions, blink, look, walk/run, map carry, accessories).
7. **IDENTITY COMPATIBILITY** — confirm this is still Pip or Goat, not a substitute character.
8. **DEFORMATION EVIDENCE** — attach references. Do not invent a pass from synthetic fixtures.
9. **HUMAN VISUAL REVIEW** of the test poses.
10. **APPROVAL RECEIPT** — only a human can admit the rig. Synthetic paperwork cannot stand in.
11. **REGISTER PRODUCTION RIG VERSION** as a new immutable version. Keep the previous version. Never auto-replace.

Until those receipts exist, the studio stays **Waiting for approved Pip / Goat production rigs**.
