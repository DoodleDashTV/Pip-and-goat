# Pip production conversion

Protected conversion copy of the official backpack Pip. This folder is not
`production-library/`, not a theatrical bind, and not a claim that the fused
mesh is production-ready.

- Source working blend stays at `../working/pip_backpack_canonical_working.blend`
- Approved inbox parts stay immutable
- Goat is not in this folder and must not be touched
- `pip_backpack_production_conversion.blend` is created by
  `scripts/tivvlejoy/convert-approved-pip.sh`
