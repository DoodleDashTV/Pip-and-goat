# Proxy policy

Pipeline tests that need a character use `PROXY_PIPELINE_BIRD` only.

- Labeled `ddp_proxy=true`, `ddp_not_pip=true`, `ddp_approved=false`
- Not Pip, not Goat, not canon
- Not written to `production-library/`
- Not bound as `THEATRICAL`
- Builder: `scripts/assets/build_pipeline_proxy.py`
