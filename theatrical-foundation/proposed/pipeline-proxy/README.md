# PROXY_PIPELINE_BIRD

Labeled stand-in for pipeline and deformation tests.

**Not Pip. Not Goat. Not approved. Not theatrical-bound.**

Build:

```bash
/usr/local/bin/blender -b -noaudio -P scripts/assets/build_pipeline_proxy.py
```
