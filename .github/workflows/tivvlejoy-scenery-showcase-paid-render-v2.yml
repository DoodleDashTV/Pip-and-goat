name: TivvleJoy Real Scenery 30s Paid Render V2

on:
  push:
    branches:
      - cursor/tivvlejoy-scenery-showcase-30s-v1-73f1
    paths:
      - 'artifacts/tivvlejoy-scenery-showcase-30s/PAID_AUTHORIZATION_V2.json'

permissions:
  contents: read

jobs:
  fail-closed-until-v2-preflight-passes:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse paid launch until V2 bridge is verified
        run: |
          echo 'V2 paid render is intentionally disabled until the cold-start-hardened bridge passes zero-cost preflight.'
          echo 'No RunPod CREATE is performed by this workflow revision.'
          exit 1
